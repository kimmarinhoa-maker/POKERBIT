# ══════════════════════════════════════════════════════════════════════
#  Poker Manager SaaS — Variáveis de Ambiente
#  Copie este arquivo para .env e preencha com os valores do seu projeto
# ══════════════════════════════════════════════════════════════════════

# ─── Supabase ────────────────────────────────────────────────────────
# Encontre no Supabase Dashboard → Settings → API

SUPABASE_URL=https://SEU-PROJECT-ID.supabase.co
SUPABASE_ANON_KEY=eyJ...seu-anon-key...
SUPABASE_SERVICE_ROLE_KEY=eyJ...seu-service-role-key...

# Conexão direta com Postgres (para migrations e scripts)
# Encontre no Supabase Dashboard → Settings → Database → Connection string (URI)
DATABASE_URL=postgresql://postgres.[project-ref]:[password]@aws-0-sa-east-1.pooler.supabase.com:6543/postgres

# ─── API ─────────────────────────────────────────────────────────────

API_PORT=3001
NODE_ENV=development

# ─── Storage ─────────────────────────────────────────────────────────
# Bucket name no Supabase Storage para uploads de XLSX
STORAGE_BUCKET=imports

# ─── Segurança ───────────────────────────────────────────────────────
# JWT Secret do Supabase (para validação server-side)
# Encontre no Supabase Dashboard → Settings → API → JWT Secret
JWT_SECRET=sua-jwt-secret-aqui

# Origens permitidas para CORS (separadas por virgula)
ALLOWED_ORIGINS=http://localhost:3000

# ─── Frontend ────────────────────────────────────────────────────
# URL do backend para chamadas do Next.js
NEXT_PUBLIC_API_BACKEND_URL=http://localhost:3001
