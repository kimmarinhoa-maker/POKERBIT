{
  "name": "poker-manager-saas",
  "version": "0.1.0",
  "description": "Poker Manager - migracao SaaS (Fase 0: golden tests)",
  "private": true,
  "workspaces": ["apps/*"],
  "scripts": {
    "dev": "npx concurrently -n api,web -c blue,green \"npm run dev --prefix apps/api\" \"npm run dev --prefix apps/web\"",
    "dev:api": "npm run dev --prefix apps/api",
    "dev:web": "npm run dev --prefix apps/web",
    "golden:run": "node tools/run_golden.js",
    "golden:compare": "node tools/compare.js",
    "golden:update": "node tools/run_golden.js --update",
    "golden:inspect": "node tools/run_golden.js --inspect",
    "test": "vitest run",
    "test:web": "npm run test --prefix apps/web",
    "test:all": "vitest run && npm run test --prefix apps/web",
    "test:watch": "vitest",
    "test:golden": "node tools/compare.js",
    "lint": "eslint apps/web/src apps/api/src --ext .ts,.tsx --max-warnings 100",
    "format:check": "prettier --check \"apps/*/src/**/*.{ts,tsx}\"",
    "format": "prettier --write \"apps/*/src/**/*.{ts,tsx}\""
  },
  "devDependencies": {
    "xlsx": "^0.18.5",
    "@testing-library/jest-dom": "^6.6.3",
    "@testing-library/react": "^16.1.0",
    "@typescript-eslint/eslint-plugin": "^8.0.0",
    "@typescript-eslint/parser": "^8.0.0",
    "eslint": "^8.57.0",
    "eslint-plugin-react-hooks": "^5.2.0",
    "jsdom": "^25.0.1",
    "prettier": "^3.3.0",
    "concurrently": "^9.0.0",
    "vitest": "^4.0.18"
  },
  "engines": {
    "node": ">=18.18.0"
  }
}
